Simple document
================
Yijia Wang

I’m an R Markdown document!

# Section 1

Here’s a **code chunk** that samples from a *normal distribution*:

``` r
samp = rnorm(100)
length(samp)
```

    ## [1] 100

# Section 2

I can take the mean of the sample, too! The mean is 0.120967.

# section 5

This plot shows the distribution of the $X$ （LaTeX 数学格式）

``` r
library(tidyverse)
```

    ## ── Attaching core tidyverse packages ──────────────────────── tidyverse 2.0.0 ──
    ## ✔ dplyr     1.2.1     ✔ readr     2.2.0
    ## ✔ forcats   1.0.1     ✔ stringr   1.6.0
    ## ✔ ggplot2   4.0.3     ✔ tibble    3.3.1
    ## ✔ lubridate 1.9.5     ✔ tidyr     1.3.2
    ## ✔ purrr     1.2.2     
    ## ── Conflicts ────────────────────────────────────────── tidyverse_conflicts() ──
    ## ✖ dplyr::filter() masks stats::filter()
    ## ✖ dplyr::lag()    masks stats::lag()
    ## ℹ Use the conflicted package (<http://conflicted.r-lib.org/>) to force all conflicts to become errors

``` r
set.seed(0)
samp1=tibble(
  num_var=rnorm(n=500,mean=1),
  log_var=num_var>0, #either True or False
  abs_var=abs(num_var),
  
)

ggplot(samp1,aes(x=abs_var))+geom_histogram()
```

    ## `stat_bin()` using `bins = 30`. Pick better value `binwidth`.

![](0915_files/figure-gfm/unnamed-chunk-2-1.png)<!-- -->

The median is 0.94.

``` r
round(median(pull(samp1,num_var)),2)
```

    ## [1] 0.94

# section 6

# 1st Level Header

## 2nd Level Header

**bold** or **bold** *italic* or *italic* `code` superscript<sup>2</sup>
and subscript<sub>2</sub>

# section 7: learning assessment 3

- The median is 0.94.
